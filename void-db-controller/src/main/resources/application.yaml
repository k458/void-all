spring:
  application:
    name: void-db-controller
  datasource:
    url: jdbc:postgresql://void-db:5432/void-db
    username: u
    password: p
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true